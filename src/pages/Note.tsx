import WordSaved from '../components/WordSaved';
import '../styles/pages/Note.scss';

const Note = () => {
  return (
    <div className='note'>
      <WordSaved />
    </div>
  );
};

export default Note;
